# 【連載第5回】アナログ時計風の円グラフで一日を可視化！～ダッシュボード編～

## 前回のおさらい

タスク管理機能が完成しました！
今回は、一日の作業を**アナログ時計風の円グラフ**で可視化していきます。

## アナログ時計のコンセプト

### なぜ時計風？
- **24時間 = 1日**を直感的に理解
- **AM/PM**で自然に生活リズムを把握
- **集中時間・休憩時間・その他**を色分け表示

## Canvas APIで描画

### 基本セットアップ
```javascript
renderDashboard() {
    const canvas = document.getElementById('productivity-chart');
    const ctx = canvas.getContext('2d');
    const centerX = canvas.width / 2;
    const centerY = canvas.height / 2;
    const radius = 150;
}
```

### 24時間データの準備
```javascript
const totalMinutes = 24 * 60; // 1440分
const focusTime = this.dailyStats.totalFocusTime;
const breakTime = Math.floor(focusTime * 0.2); // 休憩は作業の20%
const otherTime = totalMinutes - focusTime - breakTime;

const data = [
    { label: '集中時間', value: focusTime, color: '#ff6b6b' },
    { label: '休憩時間', value: breakTime, color: '#4CAF50' },
    { label: 'その他', value: otherTime, color: '#e0e0e0' }
];
```

## 円グラフの描画

### セクション毎に描画
```javascript
let currentAngle = -Math.PI / 2; // 12時方向から開始

data.forEach(item => {
    const sliceAngle = (item.value / totalMinutes) * 2 * Math.PI;
    
    ctx.beginPath();
    ctx.moveTo(centerX, centerY);
    ctx.arc(centerX, centerY, radius, currentAngle, currentAngle + sliceAngle);
    ctx.closePath();
    ctx.fillStyle = item.color;
    ctx.fill();
    
    currentAngle += sliceAngle;
});
```

## アナログ時計の装飾

### 時間マーカー（1-12時）
```javascript
for (let i = 0; i < 12; i++) {
    const angle = (i * Math.PI) / 6 - Math.PI / 2;
    const x1 = centerX + Math.cos(angle) * (radius - 10);
    const y1 = centerY + Math.sin(angle) * (radius - 10);
    const x2 = centerX + Math.cos(angle) * (radius - 20);
    const y2 = centerY + Math.sin(angle) * (radius - 20);
    
    // マーカー線を描画
    ctx.beginPath();
    ctx.moveTo(x1, y1);
    ctx.lineTo(x2, y2);
    ctx.stroke();
    
    // 時間ラベル
    const hour = i === 0 ? 12 : i;
    const labelX = centerX + Math.cos(angle) * (radius - 35);
    const labelY = centerY + Math.sin(angle) * (radius - 35);
    ctx.fillText(hour.toString(), labelX, labelY);
}
```

### 中央に統計表示
```javascript
ctx.fillStyle = '#333';
ctx.font = 'bold 16px Arial';
ctx.textAlign = 'center';
ctx.fillText(`${this.dailyStats.completedPomodoros} ポモドーロ`, centerX, centerY - 10);
ctx.font = '12px Arial';
ctx.fillText(`${focusTime}分集中`, centerX, centerY + 10);
```

## 統計カードも追加

### 3つの主要指標
```html
<div class="daily-summary">
    <div class="stat-card">
        <div class="stat-value" id="completed-pomodoros">0</div>
        <div class="stat-label">完了ポモドーロ</div>
    </div>
    <div class="stat-card">
        <div class="stat-value" id="total-focus-time">0分</div>
        <div class="stat-label">総集中時間</div>
    </div>
    <div class="stat-card">
        <div class="stat-value" id="completed-tasks">0</div>
        <div class="stat-label">完了タスク</div>
    </div>
</div>
```

### グラデーション効果
```css
.stat-card {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    padding: 20px;
    border-radius: 10px;
    text-align: center;
}
```

## 今日のポイント

✅ **Canvas API**で自由自在な描画
✅ **アナログ時計**で直感的な時間把握
✅ **統計カード**で数値の見える化

## 次回予告

次回は「モード切替編」！
**計画的遂行 vs だらだらモード**の実装で、作業の質を可視化します。

**読了時間: 約5分**

---

**#Canvas #DataVisualization #Dashboard #AnalogClock #統計**

> 自分の一日がどんな風に見えるか確認してみよう！
> 意外な発見があるかも 📊🕐