# 【連載第3回】25分カウントダウン＆円形プログレスバーを実装！～タイマー機能編～

## 前回のおさらい

HTML骨格とCSSデザインが完成しました！
今回はいよいよ、ポモドーロタイマーの**心臓部分**を作っていきます。

## タイマーの仕組み

### 基本設計
- **25分 = 1500秒**でカウントダウン
- **1秒ごと**に表示を更新
- **円形プログレスバー**で進捗を視覚化

### JavaScript クラス設計

```javascript
class PomodoroTimer {
    constructor() {
        this.workDuration = 25 * 60; // 25分（秒）
        this.shortBreakDuration = 5 * 60; // 5分
        this.longBreakDuration = 15 * 60; // 15分
        this.currentTime = this.workDuration;
        this.isRunning = false;
    }
}
```

## 円形プログレスバーの実装

### CSS conic-gradient マジック✨

```css
.timer-circle {
    background: conic-gradient(
        from 0deg,
        #ff6b6b 0deg,
        #ff6b6b 0deg,  /* 進捗部分 */
        #e0e0e0 0deg,  /* 未完了部分 */
        #e0e0e0 360deg
    );
}
```

### JavaScriptで動的更新

```javascript
updateProgressCircle() {
    const progress = ((totalDuration - this.currentTime) / totalDuration) * 360;
    
    this.timerCircle.style.background = `conic-gradient(
        from 0deg,
        #ff6b6b 0deg,
        #ff6b6b ${progress}deg,
        #e0e0e0 ${progress}deg,
        #e0e0e0 360deg
    )`;
}
```

## 時間表示の工夫

### MM:SS フォーマット
```javascript
updateDisplay() {
    const minutes = Math.floor(this.currentTime / 60);
    const seconds = this.currentTime % 60;
    this.timeDisplay.textContent = 
        `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
}
```

## タイマー制御

### スタート・ストップ機能
```javascript
startTimer() {
    this.isRunning = true;
    this.timer = setInterval(() => {
        this.currentTime--;
        this.updateDisplay();
        
        if (this.currentTime <= 0) {
            this.completeSession();
        }
    }, 1000);
}
```

## ポモドーロサイクル

### 作業 → 休憩 → 作業...
- 4回目のポモドーロ後は**長い休憩（15分）**
- それ以外は**短い休憩（5分）**

## 今日のポイント

✅ **setInterval**で1秒ごとの更新
✅ **conic-gradient**で美しい円形プログレス
✅ **時間計算**のロジック実装

## 次回予告

次回は「タスク管理編」！
改行でタスク追加、完了機能を実装します。

**読了時間: 約4分**

---

**#JavaScript #Timer #CSS #conic-gradient #ポモドーロテクニック**

> 実際に25分測ってみよう！
> 集中力アップ間違いなし 🍅⏰