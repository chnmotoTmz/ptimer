# 【連載第4回】改行でサクサクタスク追加！完了機能も実装～タスク管理編～

## 前回のおさらい

25分タイマーと円形プログレスバーが完成！
今回は、ポモドーロと組み合わせる**タスク管理機能**を作っていきます。

## UX重視のタスク入力

### 改行で自動追加
普通のTodoアプリは「追加ボタン」を押す必要がありますが...
**改行するだけ**でタスクが追加されるように！

```javascript
taskInput.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) {
        this.addTasks();
    }
});
```

### 複数行対応
```javascript
addTasks() {
    const input = this.taskInput.value.trim();
    const taskTexts = input.split('\n').filter(text => text.trim());
    
    taskTexts.forEach(taskText => {
        const task = {
            id: Date.now() + Math.random(),
            text: taskText.trim(),
            completed: false,
            createdAt: new Date().toISOString()
        };
        this.tasks.push(task);
    });
}
```

## タスクの表示

### 動的レンダリング
```javascript
renderTasks() {
    this.taskList.innerHTML = '';
    
    this.tasks.forEach(task => {
        const li = document.createElement('li');
        li.className = `task-item ${task.completed ? 'completed' : ''}`;
        li.innerHTML = `
            <span class="task-text">${this.escapeHtml(task.text)}</span>
            <div class="task-actions">
                <button onclick="pomodoroTimer.completeTask('${task.id}')">完了</button>
                <button onclick="pomodoroTimer.deleteTask('${task.id}')">削除</button>
            </div>
        `;
        this.taskList.appendChild(li);
    });
}
```

## CSS でスタイリッシュに

### ホバー効果
```css
.task-item:hover {
    transform: translateX(5px);
    background: #e8f4f8;
}
```

### 完了タスクの見た目
```css
.task-item.completed {
    opacity: 0.7;
}

.task-item.completed .task-text {
    text-decoration: line-through;
    color: #888;
}
```

## データ永続化

### LocalStorage 活用
```javascript
saveData() {
    const data = {
        tasks: this.tasks,
        dailyStats: this.dailyStats,
        lastSaved: new Date().toISOString()
    };
    localStorage.setItem('pomodoroTimer', JSON.stringify(data));
}
```

### 日付チェック機能
```javascript
loadData() {
    const savedData = localStorage.getItem('pomodoroTimer');
    if (!savedData) return;
    
    const data = JSON.parse(savedData);
    
    // 日付が変わっていたら統計をリセット
    const today = new Date().toDateString();
    const savedDate = data.dailyStats.startTime ? 
        new Date(data.dailyStats.startTime).toDateString() : null;
    
    if (savedDate !== today) {
        // 新しい日の始まり！
        this.resetDailyStats();
    }
}
```

## XSS対策も忘れずに

### エスケープ処理
```javascript
escapeHtml(text) {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
```

## 今日のポイント

✅ **UX重視**の改行自動追加
✅ **LocalStorage**でデータ永続化
✅ **セキュリティ**も考慮したHTMLエスケープ

## 次回予告

次回は「ダッシュボード編」！
アナログ時計風の円グラフで一日の負荷量を可視化します。

**読了時間: 約4分**

---

**#TaskManagement #LocalStorage #UX #JavaScript #セキュリティ**

> タスクをどんどん追加して試してみよう！
> 改行だけで追加できるの、めちゃくちゃ便利 📝✨